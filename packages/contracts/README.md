# Hot Hands Contracts

Sui Move package for the minimal social proof layer.

MVP events:

- `ProfileCreated`
- `SignalPosted`
- `CopyRuleArmed`
- `CopyReceipt`
- `Followed`

DeepBook Predict remains the trading execution layer.

Stage 3 transaction work comes after the read-only Predict canary. Do not treat
the current testnet package/object IDs as mainnet-stable, and do not document
mint readiness until transaction snapshots, wallet flow reads, and dry-run
coverage exist.
