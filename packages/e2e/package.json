{
  "name": "@hot-hands/e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test realtime/realtime-activity-stream.test.ts && playwright test && bun run test:live",
    "test:live": "playwright test --config playwright.live.config.ts",
    "test:worker-live": "playwright test --config playwright.worker-live.config.ts",
    "test:realtime": "bun test realtime/realtime-activity-stream.test.ts"
  },
  "devDependencies": {
    "@hot-hands/demo-runner": "workspace:*",
    "@hot-hands/shared": "workspace:*",
    "@playwright/test": "^1.60.0"
  }
}
