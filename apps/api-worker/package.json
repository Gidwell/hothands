{
  "name": "@hot-hands/api-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "bun build src/index.ts --target=browser > /dev/null",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"TODO: add API worker lint\"",
    "test": "echo \"TODO: add Durable Object protocol tests\"",
    "test:worker": "echo \"TODO: add Cloudflare Workers Vitest tests\""
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260509.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.15.2"
  }
}
